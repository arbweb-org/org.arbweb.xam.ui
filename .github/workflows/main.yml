name: CICD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2

    - name: build the solution
      env:
        nuget_key: ${{ secrets.arbweb_nuget_key }}
      run: |
        cd "org.arbweb.xam.ui"
        ls
        nuget restore
        msbuild org.arbweb.xam.ui.sln /p:Configuration=Release
        nuget pack org.arbweb.xam.ui.nuspec
        
        nuget setapikey "$nuget_key"
        nuget push *.nupkg -ApiKey oy2jl55ohcsq6fhbxxfacyb5z6qduqwuxm3ckuukurprkm -Source https://api.nuget.org/v3/index.json -SkipDuplicate
